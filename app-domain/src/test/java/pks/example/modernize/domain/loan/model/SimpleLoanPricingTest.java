package pks.example.modernize.domain.loan.model;

import java.sql.Date;

import org.javamoney.moneta.Money;
import org.junit.jupiter.api.Test;

import pks.example.modernize.domain.loan.model.Loan;

public class SimpleLoanPricingTest {
    
    // @Test
    // void SimpleTest() {
	// 	LendingCommitment commitment = new LendingCommitment();
	// 	LoanTermSheet terms = new LoanTermSheet();
	// 	PricingStrategy pricer = new PriceSimpleLoan();
	// 	LoanContract loan;
	// 	if (pricer.canUse(commitment, terms)) {
	// 		loan = pricer.priceLoan(commitment, terms).orElse(null);
	// 	}

	// 	if (true) {
	// 		commitment.addLoan()
	// 	}

    // }

	// {
	// 	FundingContract contract = Storage.retrieveFundingById(id);
	// 	LoanTermSheet terms = contract.getLoanTermSheet

	// }
}




